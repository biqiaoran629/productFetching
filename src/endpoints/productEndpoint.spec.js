describe('ProductEndPoints', () => {
  /*
  Some Missing Tests

  idea is to have 100% coverage, with good path and bad path both present

  it("should return 400 response code if asid not present")
  it("should return product asid is valid")
  it("should handle errors in catch and return respective error codes/responses")
  */
});